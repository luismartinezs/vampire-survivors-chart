We need to make the current behavior be like expected behavior

We take as example the following pair of relations:

A: santa_water + attractorb -> la_borra
B: la_borra + hydroStorm -> hydroPumpClimax

**Current** behavior of settings passivesShowUnions, weaponsShowUnions and unionsShowBaseItems

- passivesShowUnions: it is correct expected behavior
- weaponsShowUnions
  - if true: when santa_water is selected, it shows only A
  - if false: when santa_water is selected, it shows only A
- unionsShowBaseItems
  - if true