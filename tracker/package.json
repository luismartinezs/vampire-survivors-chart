{
  "name": "vsevochart-tracker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute vsevochart-clicks --remote --file=./schema.sql",
    "db:local": "wrangler d1 execute vsevochart-clicks --local --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20260620.1",
    "wrangler": "4.103.0"
  }
}
